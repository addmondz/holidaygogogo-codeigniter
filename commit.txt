feat(faq,agent-score): FAQ bulk import/export and Owner+TC Lead score inclusion

- Add Faq/Export_Template: downloads all internal FAQs as an editable Excel
  template (columns: FAQ, DESTINATION, QUESTION, ANSWER, TAGS); OWNER only
- Add Faq/Import: wipe-and-rebuild internal FAQ library from an uploaded
  template; wraps Replace_Internal() in a transaction so a failure leaves the
  old library intact; keeps the newest 3 uploads as backups
- Add Faq_Model::Parse_Import(): pure, DB-free inverse of Export_Rows() that
  regroups flat sheet rows back into FAQ structures (blank-title carry-forward,
  destination union/dedup, header-row detection)
- Add Faq_Model::Prune_Backups(): pure, filesystem-free helper that returns
  filenames to delete, keeping only the newest N uploads (sorted by embedded
  unix timestamp)
- Add Faq_Model::Wipe_Internal() and Replace_Internal(): soft-delete + transactional
  rebuild of the internal FAQ library; auto-creates missing tags, skips unknown
  destinations, and reports a creation summary
- Expand Agent Score population to include Owner (Level 10) and TC Lead (Level 25):
  they are scored and benchmark (set the 100-anchors alongside Level 20) but
  are hidden from the leaderboard and owner matrix via a new 'hidden' flag
- Update agent_score_helper 'hidden' flag: hidden agents still anchor but are
  omitted from ranked output, by_admin, total, and top
- Update owner_agent_matrix_build() with $hidden_admins param: hidden rows are
  included in score computation then dropped from the returned matrix
- Update Agent_Score_Setting_Model::Sales_Agents() to include Level 10 & 25
- Update agent score settings view: explain hidden behaviour, add role labels
  for OWNER / TC / TC LEAD / TC2
- Add FAQ index Export/Import buttons and import modal with wipe-and-replace
  confirmation guard; spinner on submit
- Add tests: FaqParseImportTest.php, FaqPruneBackupsTest.php (new), plus hidden
  flag cases in AgentScoreNormalizeTest and OwnerAgentMatrixMergeTest

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
