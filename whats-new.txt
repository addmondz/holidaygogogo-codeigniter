- FAQ Export: Owner can download all internal FAQs as an editable Excel template
- FAQ Import: Owner can upload an edited template to rebuild the whole internal FAQ library (wipe-and-replace, transactional)
- Import keeps the last 3 uploaded files as backups; older ones are auto-pruned
- New tags from the import file are auto-created; unknown destinations are skipped with a report
- Import confirmation dialog and spinner guard prevent accidental double-submits
- Owner (Level 10) and TC Lead (Level 25) are now included in Agent Score calculation
- Owner and TC Lead set the 100-benchmark anchors alongside sales agents but are NOT shown on the leaderboard or owner matrix
- New 'hidden' flag in agent_score_compute: hidden agents anchor the bar without appearing in ranked output
- owner_agent_matrix_build gains a $hidden_admins param; hidden rows are scored then dropped from matrix display
- Agent Score Settings page now lists Owner and TC Lead so they can be excluded from the calculation entirely if needed
- Role labels on settings page updated: OWNER / TC / TC LEAD / TC2
- Tests: two new test files for Parse_Import and Prune_Backups; hidden-flag cases added to existing score and matrix tests
